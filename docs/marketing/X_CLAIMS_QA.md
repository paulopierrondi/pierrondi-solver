# X Claims QA — fact-check of X_LAUNCH_PACK.md

Date: 2026-07-26 · Scope: every public claim in `docs/marketing/X_LAUNCH_PACK.md`
checked against the repository. `origin/main` = public state; working tree
(branch `feat/multi-engine-mcp-proxy-hcaptcha`) is ahead. Read-only audit.

| # | Claim (launch pack line) | Status | Evidence |
|---|---|---|---|
| 1 | Local strategies for reCAPTCHA v2/v3, hCaptcha, Turnstile exist and are on main (L20-23, L63, L67) | **VERIFIED** | `origin/main:src/pierrondi_solver/strategies/recaptcha_v2_audio.py` (audio + faster-whisper, "$0", docstring L1-12), `recaptcha_v3.py` (local stealth browser harvest, docstring L1-3), `hcaptcha.py` (accessibility cookie → audio → faster-whisper, docstring L1-12), `turnstile.py` (local stealth harvest, docstring L1-3). All four present in `git ls-tree origin/main`. |
| 2 | Latencies v2 14.1s / v3 4.5s / hCaptcha 7.2s / Turnstile 4.5s (L4, L20-23, L44-47, L66) | **VERIFIED** (local evidence only — not committed) | `data/telemetry.db` (gitignored, `.gitignore:1`) records the 2026-07-30 matrix run: `recaptcha_v2/v2_audio 14114ms ok @11:34:56`, `recaptcha_v3/v3_execute 4451ms ok @11:35:00`, `hcaptcha/hcaptcha_audio 7161ms ok @11:35:08`, `turnstile/turnstile_harvest 4515ms ok @11:35:12`. Mirrors `videos/solver-local-zero/BRIEF.md:8` and `STORYBOARD.md:50,96`. Caveats: (a) DB is local-only, so the receipt is not publicly reproducible from the repo; (b) later runs show hCaptcha failures (`16157ms fail @2026-07-31`) and a 52s Cloudflare solve — quoted numbers are one good run, not medians (claim "7.2s median" in X_POSTS_BATCH1.md:97 is **not** supported: hcaptcha avg across 5 recorded attempts is 9257ms, 3/5 success). |
| 3 | Go client at `clients/go` with validation, artifact policy, redacted errors, zero implicit retry, state_change exactly once (L28, L74) | **VERIFIED** | `origin/main:clients/go/{client.go,types.go,validate.go,errors.go,client_test.go,go.mod,README.md}`. README "Guarantees" section matches claim verbatim; `client.go:27-29` (no automatic retry, state_change double-submit warning), `client.go:182-189` (`checkArtifactPolicy`), `client.go:112,152` (`redactURLError`). CI: `.github/workflows/go-client.yml`. **However the Node half of the claim is WRONG — see #11.** |
| 4 | MCP server exists (L28, L76) | **VERIFIED** | `origin/main:src/pierrondi_solver/mcp_server.py` (FastMCP, tools `solve_challenge`/`detect_challenge`/`get_browser_session`/`service_health`/`service_doctor`), `origin/main:docs/MCP_SERVER.md`, entry point `pyproject.toml:45` (`pierrondi-solver-mcp`), `CHANGELOG.md` [Unreleased] MCP entry, `tests/test_mcp_server.py`. |
| 5 | WAF detector routes DataDome/Queue-it/Akamai/PerimeterX to STOP (L69-71, L82) | **VERIFIED** | `src/pierrondi_solver/waf.py:33-40`: `_ROUTE` maps `datadome`, `perimeterx`, `akamai`, `queue_it` → `Action.stop`; `cloudflare` → `solver_clearance`; `none` → `proceed`. Passive signals only (body/headers, waf.py:42-71). File + `tests/test_waf.py` both on `origin/main`. |
| 6 | Proxy layer: 8-char fingerprint, fail-closed (X_POSTS_BATCH1.md:89; pack L4/identity claims) | **VERIFIED** (with scope caveat) | Fingerprint: `src/pierrondi_solver/proxy.py:44-46` — `sha256(connect_string)[:8]`, non-reversible, telemetry-only. Fail-closed: `src/pierrondi_solver/strategies/cloudflare_clearance.py:114-131` — `_resolve_proxy` returns `deps_missing` outcome when `proxy_required` and resolution failed, or when backend `supports_proxy=False` (cf. `browser/nodriver_backend.py:41-42`); covered by `tests/test_proxy_strategies.py:76,101` (`*_fails_closed_when_proxy_required_but_unavailable`). Caveat: when proxy is optional (default), a rotating-endpoint failure returns `None` and the harvest proceeds on the host IP — fail-closed only holds with `SOLVER_PROXY_*` required or unsupported-backend paths. |
| 7 | "MIT" license (L28, L39, L49, L76) | **VERIFIED** | `LICENSE:1` ("MIT License", Copyright 2026 Paulo Pierrondi), `pyproject.toml:10-11` (`license = "MIT"`). |
| 8 | Cloudflare clearance is session-bound (L24, thread artifact-policy claim L74) | **VERIFIED** | `src/pierrondi_solver/models.py:52-58`: cloudflare → `consumption = "session_bound"`, `must_not_reuse_across_purposes = False`. Binding invariant documented in `strategies/cloudflare_clearance.py:3-7` (cf_clearance bound to UA + IP; caller must reuse both) and `proxy.py:53-54`; response `extra.usage` instructs same UA + same IP (cloudflare_clearance.py `extra` dict). |
| 9 | "The mcp-2.0.0 package on PyPI is not the MCP SDK" — typosquat story (L98); QA asked for a CHANGELOG pin note | **WRONG** | (a) No pin note exists: `CHANGELOG.md` has zero mentions of typosquat/pin/2.0.0; the dep spec is `pyproject.toml:62` `mcp>=1.2`, which does **not** exclude 2.x. (b) The factual premise is false as of 2026-07-26: live PyPI shows `mcp` **2.0.0 is the official Model Context Protocol SDK** (author "Model Context Protocol, a Series of LF Projects, LLC", repo `github.com/modelcontextprotocol/python-sdk`). Publishing this video as scripted would spread a false security claim. Kill or rewrite (e.g. "we floor-pin `mcp>=1.2` and verify the SDK origin"). |
| 10 | "Telemetry never stores raw tokens" (X_POSTS_BATCH1.md:89; pack privacy posture) | **VERIFIED** | `src/pierrondi_solver/telemetry.py:39-40` (`token_fingerprint` = `sha256(token)[:12]`), `:107` (only `token_hash` inserted); schema `:10-29` has no token column. Note: token fingerprint is 12-char; the "8-char fingerprint" quote refers to the **proxy** fingerprint (claim #6) — X_POSTS_BATCH1.md:89 conflates the two. |
| 11 | **(bonus, blocking)** "Go + Node clients" (L28) / "Node (solver-client.mjs)" (L74) | **WRONG** | No `solver-client.mjs` (or any `.mjs`/Node client) exists in the working tree or on `origin/main`; `clients/` contains only `go/`. Thread tweet 4's "same validation, artifact policy, redacted errors, zero implicit retry" is verified **only for Go**. Remove the Node mention or ship the client before launch. |

## Summary

- **VERIFIED:** 1, 2 (local-only evidence), 3, 4, 5, 6, 7, 8, 10.
- **WRONG:** 9 (mcp 2.0.0 *is* the official SDK; no pin note exists), 11 (Node client does not exist).
- **Needs rewording:** "7.2s median" (X_POSTS_BATCH1.md:97) — recorded hCaptcha average is ~9.3s with 3/5 success; the pack itself quotes single-run values, which is fine if framed as "live run today", not "median".
- Evidence caveat: latency receipts live in gitignored `data/telemetry.db`; consider committing a redacted matrix run log under `docs/research/` so the receipts survive repo-only inspection.
